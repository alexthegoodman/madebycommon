{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "npm --prefix ./packages/common-server run build",
    "start": "npm --prefix ./packages/common-server run serve",
    "postinstall": "npm --prefix ./packages/common-server install"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
